# MnesOS Claude Code Instructions

To maintain a single source of truth across all assistants, core instructions are modularized. Before generating plans or modifying code, use your file reading tools to view `docs/ai-index.md` and load only the relevant documentation domain files relative to the `docs/` directory.
